services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: slumbr
      POSTGRES_PASSWORD: slumbr_secret
      POSTGRES_DB: slumbr
    ports:
      - '5432:5432'
    volumes:
      - slumbr_db:/var/lib/postgresql/data

volumes:
  slumbr_db:
